{
	"name": "sumviewer",
	"displayName": "SumViewer",
	"description": "",
	"version": "0.0.1",
	"publisher": "CouturierValentin",
	"engines": {
		"vscode": "^1.34.0"
	},
	"categories": [
		"Other"
	],
	"activationEvents": [
		"onCommand:extension.computeSum"
	],
	"main": "./out/extension.js",
	"contributes": {
		"commands": [
			{
				"command": "extension.computeSum",
				"title": "Compute sums"
			}
		]
	},
	"scripts": {
		"vscode:prepublish": "npm run compile",
		"compile": "tsc -p ./",
		"watch": "tsc -watch -p ./",
		"postinstall": "node ./node_modules/vscode/bin/install",
		"test": "npm run compile && node ./node_modules/vscode/bin/test"
	},
	"devDependencies": {
		"@coutcout/tree-datastructure": "^1.0.9",
		"@types/mocha": "^2.2.42",
		"@types/node": "^10.14.9",
		"tslint": "^5.17.0",
		"typescript": "^3.5.2",
		"vscode": "^1.1.28"
	},
	"dependencies": {}
}
